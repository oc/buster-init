#!/usr/bin/env node
require("../lib/init").create().run(process.argv.slice(2));

// var args = Object.create(require("buster-args"));
// 
// var project = args.createOption("-p", "--project");
// project.hasValue = true;
// project.defaultValue = undefined;
// project.help = "\tSet project type. By default it will try to detect the project.\n" +
//             "\t\tplain   Plain project - fallback if it cannot detect" +
//             "\t\tmaven   Maven project";
// prefix.addValidator(args.validators.inEnum(["plain", "maven"]));
//             
// var style = args.createOption("-s", "--style");
// style.hasValue = true;
// style.defaultValue = undefined;
// style.help = "\tSelect test type style. By default it will try to detect the style.\n" +
//             "\t\txunit   xUnit style tests - fallback if it cannot detect" +
//             "\t\tbdd     BDD style specs";
// prefix.addValidator(args.validators.inEnum(["xunit", "bdd"]));
// 
// var prefix = args.createOperand();
// prefix.defaultValue = ".";
// prefix.help = "\tSet the project prefix. By default it will use current working dir (.).\n" +
//            "\t\tplain   Plain project" +
//            "\t\tmaven   Maven project";
// prefix.addValidator(args.validators.directory());
// 
// var help = args.createOption("-h", "--help");
// help.help = "\tShow this information."
// 
// args.handle(process.argv.slice(2), function (errors) {
//     if (errors) {
//         console.log(errors[0]);
//         return;
//     }
//     
//     if (help.isSet) {
//         console.log("Buster init - Initializes Buster.js projects");
//         console.log("Will try to detect your project and style.\n");
//         console.log("Usage:");
//         console.log("\tbuster init [-p <project>] [-s <style>] [prefix]");
//         console.log("\nOptions:");
//         console.log(project.signature + project.help);
//         console.log(style.signature + style.help);
//         console.log(help.signature + help.help);
//         return;
//     }        
//     
//     require("../lib/init").init(project.value, style.value, prefix.value);
// });
// 
